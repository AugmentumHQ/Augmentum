# Provider Corrections — Verified Backlog

> Grows every time a provider card surfaces **verified, actionable** drift between what the
> provider's official docs say and what Augmentum actually does. "Verified" = confirmed against
> a primary source (provider docs) on the date noted, not inferred.
>
> This is the actionable companion to [`README.md`](README.md) (the card index) and complements
> the snapshot in [`../provider-integration-matrix.md`](../provider-integration-matrix.md).
>
> **Severity:** 🔴 high (hard failure / data loss) · 🟡 medium (silent degradation) · 🟢 low (nicety / cosmetic).
> **Status:** ☐ open · ✅ resolved.

---

## Open

| # | Provider | Severity | Finding | Fix location | Verified (source, date) |
|---|---|:--:|---|---|---|
| 4 | OpenRouter | 🟡 | Unified `reasoning` control object never emitted (reaches only OpenAI-family ids) → Anthropic/DeepSeek/Qwen/GLM via OR get no reasoning control. **IMPLEMENTED + unit-tested (7 tests), LIVE-VERIFY PENDING:** flag `supports_openrouter_reasoning` (on `openrouter`) + `_openrouter_reasoning(think, effort)` emits `reasoning:{effort}` (low/med/high, UI tiers clamped) / `{enabled:true/false}`. ⚠️ confirm `{enabled:false}` (disable) is accepted live → `scripts/verify_openrouter_reasoning.py` (free model, zero cost). | `openai_compat.py` `_openrouter_reasoning` + `provider_profiles.py` `openrouter` | openrouter.ai/docs/guides/best-practices/reasoning-tokens · 2026-06-25 |
| 5 | Mistral | 🟢 | Seed param name mismatch: Mistral uses `random_seed`, OpenAI uses `seed` → if we emit `seed`, determinism silently lost. | `converters/mistral` seed mapping | docs.mistral.ai/api · 2026-06-25 |
| 6 | Mistral | 🟢 | Mistral has no `reasoning_effort`/`thinking` toggle — reasoning is model-selection (`magistral-*` / Small 4). Ensure role/UI surfaces those instead of expecting a toggle. | role resolver / UI | docs.mistral.ai/api · 2026-06-25 |
| 8 | AI21 | 🟡 | Jamba requires `stream=false` when `tools` is specified → streaming tool turns fail. **Deferred** (siblings #7/#9 resolved 2026-07-01; this one needs request-path branching for a provider with no built-in models — low blast radius). | request path (force non-stream for ai21 tool calls) | docs.ai21.com/reference/jamba-1-6-api-ref · 2026-06-25 |
| 14 | Moonshot | 🟢 | Kimi supports `prompt_cache_key` + cache-aware billing, but profile `supports_prompt_cache_key=False` → never sends a key → misses cheaper cache-hit tier ($0.16 vs $0.95). | `provider_profiles.py` `moonshot` | platform.kimi.ai/docs/pricing/chat-k26 · 2026-06-25 |
| 19 | Perplexity | 🟢 | Search surface unused — `search_mode`/recency/domain/date filters, `web_search_options.search_context_size` (drives per-request price), `return_related_questions`, citations none surfaced → Sonar runs as plain chat, loses grounding, hides per-request search billing. | new search-control passthrough | docs.perplexity.ai/api-reference · 2026-06-25 |
| 21 | Fireworks | 🟢 | Supports `prompt_cache_key` (cached ~80–90% cheaper) + `service_tier` (auto/default/flex/priority); bare profile sends neither → misses cache discount + tier control. | `provider_profiles.py` `fireworks` | docs.fireworks.ai/api-reference · 2026-06-25 |
| 23 | Pollinations | 🟢 | Profile base URL `https://gen.pollinations.ai/v1` differs from the documented text endpoint `https://text.pollinations.ai/openai` → verify the configured host still resolves; anon tier is 1 req/15s without a `referrer`/registration. | `provider_profiles.py` `pollinations.base_url` | github.com/pollinations/pollinations/blob/master/APIDOCS.md · 2026-06-25 |
| 27 | Z.AI | 🟢 | Profile sends only the binary `thinking:{type:enabled/disabled}` toggle; GLM-4.5+ also accepts `reasoning_effort` (`max/high/medium/low/minimal/none`) for finer control → unused. (Also: GLM-4.5V/5V return in-band `<think>`/`<\|begin_of_box\|>` tags in content, not `reasoning_content` — vision path needs tag extraction.) | `provider_profiles.py` `zai` + effort emit | docs.z.ai/guides/llm/glm-4.6 · 2026-06-25 |
| 28 | Cohere | 🟡 | Via the OpenAI-shaped `converter_id="cohere"`, Cohere v2's differentiators — `documents`/`citations` (grounded RAG), `safety_mode` (CONTEXTUAL/STRICT/OFF), and the `thinking` **content-block** return — are likely dropped → Command runs as plain chat and reasoning blocks may not be extracted (reasoning is a content block, NOT `reasoning_content`). | `converters/cohere` (verify thinking-block + tool_choice enum + documents) | docs.cohere.com/reference/chat · 2026-06-25 |
| 29 | Cohere | 🟢 | Cohere v2 uses `k`/`p` (not `top_k`/`top_p`), `tool_choice` `REQUIRED`/`NONE` (uppercase), and penalty range **0.0–1.0** (not −2..2). Converter must rename + clamp or these are ignored/rejected. Also: reasoning is **per-model** (only `command-a*`/`-reasoning`) — don't send `thinking` to `command-r`/`r7b`. | `converters/cohere` sampler map | docs.cohere.com/reference/chat · 2026-06-25 |
| 30 | Together | 🟢 | Bare profile sends no reasoning toggle → hosted reasoning models reason by default. **No actionable fix**: Together AI docs (2026-06-25) do NOT document a `reasoning_effort` / `enable_thinking` / `chat_template_kwargs` control param. Reasoning returns in `reasoning_content` (read ✅). `safety_model` hook unused (low value). Will close automatically if Together adds a toggle; re-check on next card refresh. | no fix location | docs.together.ai/docs/serverless-models · 2026-06-25 |
| 31 | Gemini | 🟢 | `get_thinking_config` Gemini-3 regexes (`gemini-3.*flash`/`gemini-3.*pro`) use an unescaped `.` → slightly loose; should be `gemini-3[.\d-]*`. Flash-Lite routes via the Flash `thinkingLevel` path — verify it honors `thinkingLevel`. Also `safetySettings` never sent (defaults may over/under-block). | `converters/gemini.py:21-22` + adapter safetySettings | ai.google.dev/gemini-api/docs/models · 2026-06-25 |

---

## Resolved (this audit)

| # | Provider | Finding | Fix | Date |
|---|---|---|---|---|
| R1 | All non-OpenAI | `system`→`developer` role rewrite leaked onto any provider wearing a borrowed `openai`/`chatgpt_bridge` profile → DeepSeek beta endpoint hard-400 (`unknown variant 'developer'`). | Gated the rewrite on `is_openai_family_model(model)` instead of the profile flag (`openai_compat.py:391`). | 2026-06-25 |
| R2 | NVIDIA + **all cloud** serving Qwen3 / GLM-4.x / DeepSeek-V4 / MiniMax / EXAONE (was #17, 🔴) | `ThinkingStreamBuffer._inside_think` inited `True` for `_STARTS_THINKING_FAMILIES` **regardless of backend** → a cloud host returning plain content had its **entire visible answer routed into the thinking channel** (response emptied). | Added `ModelBackend.is_local_engine()` (ABC default False; llama_cpp/engine→True; openai_compat/ollama→`is_local_engine_url(base_url)`). Threaded `local_engine=` into `ThinkingStreamBuffer` — gates the `_inside_think` init. Wired at both `openai_compat` streaming sites + the passthrough soft-trigger probe (also streams to the user). Fixed the **class** (every cloud provider + every construction site), not just NVIDIA. 4 regression tests in `test_thinking.py` (GLM + DeepSeek-V4 cloud stay visible; native reasoning still routes; local default unchanged). Contract tests green; zero new ruff. | 2026-06-25 |
| R3 | NVIDIA (was #25, 🔴) | NIM reasoning is the **nested `chat_template_kwargs`** (not `reasoning_effort`/top-level `enable_thinking`); never sent → **`deepseek-v4-flash`/`-pro` HANG indefinitely** (NIM strictly requires `chat_template_kwargs:{thinking:true}` to stream reasoning), Qwen/Nemotron uncontrolled. | New profile flag `reasoning_via_chat_template_kwargs` (set on `nvidia`) + helper `_nim_chat_template_kwargs(model, think)`: **DeepSeek → `{thinking:bool}` always emitted** (the hang fix, both think states); Qwen/Nemotron/EXAONE/Gemma4/MiMo → `{enable_thinking:bool}` (reuses `_template_thinking_override` gate); non-reasoning models emit nothing; GLM/Kimi excluded (uncertain NIM key); non-NVIDIA profiles never emit it. Emitted at the root of the payload (httpx raw JSON = NIM's curl shape). 11 tests in `test_nvidia_chat_template_kwargs.py`; provider suites green; ruff clean. | 2026-06-25 |
| R4 | Groq (was #15, 🟡) | Bare profile sent no reasoning control → thinking toggle a no-op on qwen3, effort uncontrolled on gpt-oss. | New profile flag `reasoning_via_groq_params` (set on `groq`) + helper `_groq_reasoning_params(model, think, effort)`: **gpt-oss → `reasoning_effort` low/medium/high** with the UI's `minimal`/`xhigh`/`max` tiers **clamped** into Groq's set (can never 400); **qwen3 → `none`/`default`** driven by the `think` toggle. `reasoning_format` intentionally NOT sent (`raw`+JSON/tools→400; the `reasoning` field + inline `<think>` are already read). Non-reasoning Groq models + non-Groq profiles emit nothing. 16 tests in `test_groq_reasoning_params.py`. | 2026-06-25 |
| R5 | DeepSeek (was #2, 🟡) | `reasoning_effort` inside `thinking:{type}` never sent. | Effort now emitted **nested** inside the existing `thinking:{type, reasoning_effort}` object when DeepSeek profile + `think=True` + effort ∈ `high`/`max`. Only DeepSeek gets it (Moonshot/Z.AI would 400). 5 tests in `test_reasoning_effort_providers.py`. | 2026-06-25 |
| R6 | Perplexity (was #18, 🟡) | `supports_reasoning_effort=False` → effort never sent to sonar-reasoning-pro/deep-research. | **Flag flip** — set `supports_reasoning_effort=True` on `perplexity` profile. Perplexity accepts `minimal` (unlike Fireworks) so `supports_reasoning_effort_minimal` stays True (default). 3 tests. | 2026-06-25 |
| R7 | Fireworks (was #20, 🟡) | Bare profile never sent effort → reasoning no-op on hosted DeepSeek/Qwen3/GLM/Kimi. | **Flag flip** — set `supports_reasoning_effort=True` + `supports_reasoning_effort_minimal=False` (Fireworks has no minimal → demoted to low). 3 tests. | 2026-06-25 |
| R8 | Pollinations (was #22, 🟡) | `reasoning_effort` never sent despite profile `supports_thinking=True`. | **Flag flip** — set `supports_reasoning_effort=True`. Pollinations accepts minimal. 3 tests. | 2026-06-25 |
| R9 | SiliconFlow (was #24, 🟡) | `enable_thinking` + `thinking_budget` never sent → toggle no-op, CoT uncapped. | New flag `reasoning_via_siliconflow_params` + helper `_siliconflow_thinking_params(model, think, effort)`: per-model reasoning-model gate (DeepSeek/Qwen3/GLM/MiniMax/Kimi only); `enable_thinking` bool tracks `think`; effort→budget mapped (`minimal`→512 through `max`→32768, clamped [128,32768]). Non-reasoning models emit nothing. 9 tests. | 2026-06-25 |
| R10 | xAI (was #10, 🟡) | `minimal` reasoning_effort sent to Grok → 400 (no Grok model accepts `minimal`). | Set `supports_reasoning_effort_minimal=False` on the `xai` profile — the existing emit-site demotion (`openai_compat.py`) rewrites `minimal`→`low`. Same handling as chatgpt_bridge/fireworks. Verified real but edge-case (only fires on explicit `minimal` pick + Grok). | 2026-07-01 |
| R11 | Moonshot (was #13, 🟢) + Z.AI (was #26, 🟡) | GLM/Kimi reject temperature/top_p outside their narrower range (Kimi temp [0,1]; GLM temp [0,1], top_p [0.01,1]) → 400. | **Class fix**: new `temperature_range`/`top_p_range` profile fields (default `None`=pass-through so local engines using temp>2 are never clamped) + emit-site clamp in `openai_compat.py`. Set on `moonshot` (temp) and `zai` (temp+top_p). Reusable for Cohere when its converter lands. | 2026-07-01 |
| R12 | Moonshot (was #12, 🟡) | Kimi 400s on `tool_choice="required"` (fires from the math reasoning flow's Compute step routed to Kimi). | New `supports_tool_choice_required` profile flag (default True) + emit-site demote `required`→`auto` when False. Set False on `moonshot`. | 2026-07-01 |
| R13 | AI21 (was #7 🟡 + #9 🟢) | `max_output=0` (unmodeled → coder over-request) and `json_schema` sent to an API that only supports `json_object`. | Profile one-liners: `max_output=4096` (Jamba's hard cap) + `supports_response_format_json_schema=False` (existing emit-site demotes to `json_object`). Note: AI21 has no built-in models today — cheap correctness for if/when it's configured. | 2026-07-01 |
| R14 | OpenAI (was #1, 🟡) | `max_context=400_000` vs docs' 1M → suspected under-use of the window. | **NO CODE CHANGE — verified NOT a bug.** The 400K cap is deliberate: it sits below `gpt-5.5-pro`'s >272K input-surcharge threshold, and the coder compactor triggers at 256K regardless. The profile comment already documents the rationale. Closed as intended. | 2026-07-01 |
| R15 | Azure (was #11, 🟡) | `?api-version=` never injected → every Azure chat call 400s (the deployment path comes from the user's base_url, but no layer added the mandatory version query param; the registered `azure_api_version` setting was dead/unread). | New `_apply_azure_api_version()` in `openai_compat.py`, applied at both `_chat_url` return points: appends `?api-version=<azure_api_version setting, default 2024-02-01>` for the `azure` profile only; honors a version already encoded in base_url (no override/dup); non-Azure untouched. Wires the previously-dead setting. 5 tests `test_azure_chat_url.py`. **Scope:** chat path only — Azure model-listing (`/models`) uses a different path shape and stays unhandled (users name deployments manually). | 2026-07-01 |

---

## Audit false positives (verified already-handled — no code change)

Caught during the reasoning-handling fix pass by reading the actual parse path (working carefully > trusting the audit note). The original cards over-claimed; corrected here for honesty.

| was # | Provider | Original claim | Reality (committed HEAD) |
|---|---|---|---|
| 3 | OpenRouter | Reasoning in `reasoning` field dropped (reads only `reasoning_content`) | **Already handled.** Both paths read `reasoning_content or reasoning` — streaming `openai_compat.py` `_iter_stream` (`delta.get("reasoning_content") or delta.get("reasoning")`) and non-streaming `_parse_openai_response` (`msg_data.get(...)`). Comment even names OpenRouter. |
| 16 | Groq | Reasoning in `message.reasoning`/`reasoning` dropped | **Already handled** by the same `reasoning_content or reasoning` fallback (streaming `delta.reasoning`, non-streaming `message.reasoning`), plus inline `<think>` via `ThinkingStreamBuffer`. Groq's `gpt-oss` is also **not** accidentally OpenAI-classed (`is_openai_family_model("openai/gpt-oss-…")` = False — starts with `openai/`, matches no `gpt-5`/`gpt-4o`/`o1` prefix). |

---

_Append to **Open** as each card is built; move rows to **Resolved** when fixed in code._
