# Core CSM-streaming deps (pinned to the fork's tested set). The base
# image already ships torch/torchaudio 2.4.0 + CUDA 12.4; we pin the rest.
# NOTE: flash-attn / vllm / triton are the fork's *demo* accelerators and
# are intentionally omitted — generate_stream() runs without them (just a
# bit slower). If the fork's import graph hard-requires one, add it here
# and rebuild; that's the expected first-build tuning step.
tokenizers==0.21.0
transformers==4.49.0
huggingface_hub==0.28.1
moshi==0.2.2
torchtune==0.4.0
torchao==0.9.0
silentcipher @ git+https://github.com/SesameAILabs/silentcipher@master
# Service deps
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
numpy>=1.24.0
